{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep '\\.py$' && jq -r '.tool_input.file_path' | xargs -I{} sh -c 'uv run ruff check --fix \"{}\" && uv run ruff format \"{}\"'"
          }
        ]
      }
    ]
  }
}
